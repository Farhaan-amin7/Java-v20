import java.util.LinkedList;
import java.util.*;

public class Linked_List_Practice {
    public static void main(String[] args) {
        LinkedList<Integer> links= new LinkedList<>();
        LinkedList<Integer> links2= new LinkedList<>();
        links.add(3);
        links.add(9);
        links.add(4);
        links.add(6);
        links.add(7);
        links.add(5);
        links2.add(100);
        links2.add(200);
        links2.add(300);
        links2.add(400);


//      System.out.println(links);
        links.addFirst(0);
        links.addLast(9);
//      links.clear();
//        System.out.println(links.contains(100));
//        System.out.println(links.element());//shows the head of the LinkedList.
//        links.offer(600);
//        System.out.println(links.lastIndexOf(600));//shows the index of the entered parameter
//        System.out.println(links.pop());//shows the first element of this LinkedList.
//        links.push(5000);//pushes the element as the head of the LinkedList.
//        links.remove();
//        System.out.println(links.peek());//retrieves the head of the linkedlist.
//        links.remove();//removes the head of the linkedlists.
//        System.out.println(links.removeFirst());//removes the first element of the linkedlist and returns it.
//        System.out.println(links.size());//returns the number of elements in this linkedlist.
//          System.out.println("This Is  Before Adding the Content of the Other List  "+ links);
//          links.addAll(links2);
//        System.out.println("this is after adding the content of the newly formed linked list"+links);
//        Object [] hey= links.toArray();
//        System.out.println("This contains the materials of the linked list named links"+Arrays.toString(hey));
//        Object[] obj = new Object[links.size()]; // -> creates an array of the same length of the linkedList
//        obj = links.toArray();
//        System.out.println(Arrays.toString(obj));
//        Collections.sort(links);
//        System.out.println(links);


    }
}